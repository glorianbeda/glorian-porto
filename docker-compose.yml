services:
  # ============================================
  # Portfolio Frontend - Nginx serving Vite static files
  # ============================================
  app:
    image: porto-app:latest
    restart: unless-stopped
    ports:
      - "${PORT:-8080}:80"
